import { ProductCategoryMenu } from './product-category-menu';

describe('ProductCategoryMenu', () => {
  it('should create an instance', () => {
    expect(new ProductCategoryMenu()).toBeTruthy();
  });
});
